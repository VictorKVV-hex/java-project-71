/*
package hexlet.code;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.Set;

public class DescriptionDiff {
    public static List<Node>  getDifferList(Set<String> allKeys, Map<String, Object> map1, Map<String,
            Object> map2, String extension) {
        List<String> diffList = new ArrayList<>();
        diffList.clear();
        for (String key : allKeys) {
            differOfMap(diffList, map1, map2, key);
        }
        return "[" + String.join(",", diffList) + "]";
    }
}
*/
